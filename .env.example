# Supabase
# Create a Supabase project, then set these values in a local .env.local (not committed)
NEXT_PUBLIC_SUPABASE_URL= 
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (for admin tasks later)
SUPABASE_SERVICE_ROLE_KEY=
